{% if comments %}
-- Shadow of _{{ table_name }}: an empty clone (WHERE 1<>1 copies the column
-- layout but no rows) that stages _{{ suffix }} changes awaiting propagation.
{% endif %}
CREATE TABLE {{ schema }}._{{ table_name }}_{{ suffix }} AS
SELECT * FROM {{ schema }}._{{ table_name }}
WHERE 1<>1;
